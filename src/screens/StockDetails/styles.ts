import { StyleSheet } from "react-native";
import colors from "../../styles/colors";

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  textSearch: {
    color: colors.white,
    fontSize: 20,
    fontWeight: "600",
    marginTop: 50,
  },
});
